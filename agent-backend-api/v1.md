# agent-backend /v1 Contract

Base URL: `http(s)://<host>:8080`

**Last Updated:** 2026-03-21

---

## 总览

### Skill 总数：32 个

| 分类 | Skills | 数量 |
|------|--------|------|
| **Test** | echo, sleep_echo | 2 |
| **RAG** | rag.query, rag.ingest, rag.ingest_from_github, rag.intake_manual_folder, rag.sync_to_repo | 5 |
| **PR** | pr.preview, pr.submit, pr.lookup | 3 |
| **Course** | course.read, courses.search | 2 |
| **HIT** | hit.teacher, hit.teachers | 2 |
| **COS** | cos.save_file, cos.delete_file, cos.list_files, cos.get_presigned_url | 4 |
| **Files** | files.upload, files.download | 2 |
| **Search** | search, aggregator.summarize | 2 |
| **Data** | data.ingest, data.ingest_batch | 2 |
| **Crawl** | crawl4ai.page, crawl4ai.site, crawl4ai.status | 3 |
| **GitHub** | github.batch_download, document.convert | 2 |
| **MCP** | mcp.list_servers, mcp.list_tools, mcp.call_tool | 3 |
| **聚合入口** | search, data.ingest | 2 |

---

## API 端点

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| GET | `/v1/skills` | 列出所有 skills |
| POST | `/v1/skills/{name}:invoke` | 调用 skill |
| GET | `/v1/jobs/{job_id}` | 获取异步 job 状态 |

---

## 统一响应格式

**成功（同步）**
```json
{"ok": true, "output": {...}}
```

**异步接受**
```json
{"ok": true, "job_id": "..."}
```

**失败**
```json
{"ok": false, "error": {"code": "...", "message": "...", "retryable": false}}
```

---

## Skill 详细定义

### Test Skills

#### `echo` - 测试回显

**输入**: 任意 JSON 对象

**输出**
```json
{
  "ok": true,
  "output": {
    "input": {...},
    "trace": {...}
  }
}
```

---

#### `sleep_echo` - 延迟测试

**输入**
```json
{
  "sleep_ms": 1000
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "input": {...}
  }
}
```

---

### RAG Skills

#### `rag.query` - 向量搜索

**输入**
```json
{
  "query": "机器学习课程",
  "top_k": 10,
  "collection": "hit_courses_2048"
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "hits": [
      {
        "id": "doc-id",
        "score": 0.95,
        "title": "机器学习基础",
        "snippet": "机器学习是人工智能的一个分支...",
        "payload": {
          "doc_id": "...",
          "chunk_id": "...",
          "source": "manual-intake",
          "category": "manual"
        }
      }
    ],
    "total": 1
  }
}
```

---

#### `rag.ingest` - RAG 数据接入（异步）

**输入**
```json
{
  "repo": "HIT-A/HITA_RagData",
  "ref": "main",
  "path_prefix": "sources/normalized",
  "collection": "hit_courses_2048",
  "source": "manual-intake",
  "dry_run": false,
  "max_files": 100,
  "max_chunks": 5000,
  "workers": 4,
  "store_in_cos": true,
  "cos_prefix": "rag-content"
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "processed_files": 10,
  "processed_chunks": 45,
  "upserted_points": 45,
  "skipped_files": 2,
  "errors_count": 0,
  "total_files": 12,
  "workers": 4
}
```

---

#### `rag.ingest_from_github` - 从 GitHub 接入 RAG（异步）

**输入**
```json
{
  "repos": [
    {"org": "HIT-A", "repo": "some-repo", "ref": "main"}
  ],
  "max_file_size": 10485760,
  "max_repo_size_mb": 500,
  "convert_to_md": true,
  "push_to_github": true,
  "target_repo": "HIT-A/HITA_RagData",
  "store_in_cos": true,
  "auto_ingest_rag": true,
  "rag_collection": "hit_courses_2048"
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "download": {
    "total_files": 25,
    "success_count": 23,
    "fail_count": 2
  },
  "cos_upload": {
    "uploaded": 23
  },
  "github_push": {
    "commit": "abc123"
  },
  "rag_ingest": {
    "status": "completed",
    "processed_chunks": 150
  },
  "pipeline_status": "completed"
}
```

---

#### `rag.intake_manual_folder` - 手动目录入库（异步）

**输入**
```json
{
  "repo": "HIT-A/HITA_RagData",
  "branch": "main",
  "intake_dir": "incoming/manual_raw",
  "intake_dirs": ["incoming/manual_raw", "incoming/skill_raw"],
  "collection": "hit_courses_2048",
  "store_in_cos": true,
  "cos_prefix": "rag-intake/raw",
  "delete_invalid": true,
  "delete_on_succeeded": true,
  "max_files": 200,
  "max_file_size_mb": 25
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "repo": "HIT-A/HITA_RagData",
  "branch": "main",
  "intake_dir": "incoming/manual_raw",
  "processed": 5,
  "skipped": 2,
  "invalid_deleted": 1,
  "source_deleted": 3,
  "embedded_chunks": 25,
  "upserted_points": 25,
  "failures_count": 0,
  "qdrant_collection": "hit_courses_2048"
}
```

---

#### `rag.sync_to_repo` - 同步 RAG 数据到仓库（异步）

**输入**
```json
{
  "sources": [
    {
      "type": "github",
      "repo": "HIT-A/some-data",
      "path_prefix": "docs",
      "file_types": [".md", ".txt"],
      "max_files": 50
    },
    {
      "type": "crawl",
      "url": "https://example.com"
    }
  ],
  "target_repo": "HIT-A/HITA_RagData",
  "target_branch": "main",
  "store_in_cos": true,
  "cos_prefix": "rag-sync",
  "auto_ingest_rag": true,
  "rag_collection": "hit_courses_2048",
  "workers": 4
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "status": "completed",
  "repo_path": "/tmp/rag-sync-xxx",
  "sources_count": 2,
  "total_files": 55,
  "total_chunks": 280,
  "commit_hash": "abc123def",
  "cos_uploaded": 55
}
```

---

### PR Skills

#### `pr.preview` - 预览课程改动（异步）

**输入**
```json
{
  "campus": "shenzhen",
  "course_code": "COMP1011",
  "ops": [
    {"op": "add_lecturer_review", "lecturer_name": "张三", "content": "很好"},
    {"op": "add_course_teacher_review", "teacher_name": "李四", "content": "优秀课程"},
    {"op": "append_course_section_item", "section_title": "课程评价", "item": {"title": "2024春", "content": "内容..."}}
  ]
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "base": {
    "org": "HIT-A",
    "repo": "hoa-courses-shenzhen",
    "ref": "main",
    "toml_path": "COMP1011.toml"
  },
  "result": {
    "readme_toml": "...",
    "readme_md": "..."
  },
  "summary": {
    "changed_files": ["COMP1011.toml", "README.md"],
    "warnings": []
  }
}
```

---

#### `pr.submit` - 提交 PR（异步）

**输入**
```json
{
  "campus": "shenzhen",
  "course_code": "COMP1011",
  "ops": [...],
  "idempotency_key": "client-generated-uuid"
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "pr_number": 42,
  "pr_url": "https://github.com/HIT-A/hoa-courses-shenzhen/pull/42",
  "branch": "agent-backend/pr/2024-03-21/abc123"
}
```

---

#### `pr.lookup` - 查询 PR 状态

**输入**
```json
{
  "org": "HIT-A",
  "repo": "hoa-courses-shenzhen",
  "number": 42
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "number": 42,
    "state": "open",
    "url": "https://github.com/HIT-A/hoa-courses-shenzhen/pull/42",
    "merged": false,
    "checks": {
      "status": "completed",
      "conclusion": "success"
    }
  }
}
```

---

### Course Skills

#### `course.read` - 读取课程 README

**输入**
```json
{
  "campus": "shenzhen",
  "course_code": "COMP1011",
  "include_toml": true
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "data": {
      "base": {
        "org": "HIT-A",
        "repo": "hoa-courses-shenzhen"
      },
      "result": {
        "readme_md": "# COMP1011\n\n课程内容...",
        "readme_toml": "..."
      }
    }
  }
}
```

---

#### `courses.search` - 搜索课程

**输入**
```json
{
  "keyword": "python",
  "campus": "shenzhen",
  "limit": 10
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "data": {
      "results": [
        {
          "code": "CS2012",
          "name": "Python 程序设计",
          "repo": "hoa-courses-shenzhen",
          "org": "HIT-A",
          "teachers": ["张三"],
          "aliases": ["Python"]
        }
      ]
    }
  }
}
```

---

### HIT Teacher Skills

#### `hit.teacher` - 查找单个老师

**输入**
```json
{
  "name": "张三",
  "pinyin": "zhangsan",
  "department": "计算机科学与技术",
  "save_to_file": false,
  "auto_ingest": false
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "success": true,
    "teacher": {
      "name": "张三",
      "pinyin": "zhangsan",
      "title": "教授",
      "department": "计算机科学与技术系",
      "email": "zhangsan@hit.edu.cn",
      "phone": "0451-86413333",
      "office": "活动中心 301",
      "research": ["人工智能", "机器学习"],
      "homepage": "https://homepage.hit.edu.cn/zhangsan",
      "profile": "个人简介..."
    },
    "homepage": "https://homepage.hit.edu.cn/zhangsan",
    "pinyin": "zhangsan"
  }
}
```

---

#### `hit.teachers` - 批量查找老师（异步）

**输入**
```json
{
  "names": ["张三", "李四"],
  "pinyins": ["zhangsan", "lisi"],
  "max_workers": 4
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "total": 2,
  "success": 2,
  "failed": 0,
  "results": [
    {
      "success": true,
      "teacher": {...}
    }
  ]
}
```

---

### COS Skills

#### `cos.save_file` - 保存文件

**输入（单文件）**
```json
{
  "key": "path/to/file.pdf",
  "content_base64": "SGVsbG8gV29ybGQ=",
  "content_type": "application/pdf"
}
```

**输入（批量）**
```json
{
  "files": [
    {"key": "file1.pdf", "content_base64": "...", "content_type": "application/pdf"},
    {"key": "file2.pdf", "content_base64": "...", "content_type": "application/pdf"}
  ]
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "file_id": "path/to/file.pdf",
    "size": 1024,
    "url": "https://hita-courses.cos.ap-guangzhou.myqcloud.com/path/to/file.pdf",
    "access_url": "https://hita-courses.cos.ap-guangzhou.myqcloud.com/path/to/file.pdf"
  }
}
```

**批量输出**
```json
{
  "ok": true,
  "output": {
    "success": true,
    "total": 2,
    "success_count": 2,
    "fail_count": 0,
    "results": [...]
  }
}
```

---

#### `cos.delete_file` - 删除文件

**输入**
```json
{
  "key": "path/to/file.pdf"
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "deleted": true,
    "key": "path/to/file.pdf"
  }
}
```

---

#### `cos.list_files` - 列出文件

**输入**
```json
{
  "prefix": "rag-content/",
  "max_keys": 100
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "files": [
      {
        "key": "rag-content/doc1.md",
        "size": 1024,
        "last_modified": "2024-03-21T10:00:00Z"
      }
    ],
    "prefix": "rag-content/",
    "count": 1
  }
}
```

---

#### `cos.get_presigned_url` - 获取预签名 URL

**输入**
```json
{
  "key": "path/to/file.pdf",
  "expires_minutes": 30
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "url": "https://hita-courses.cos.ap-guangzhou.myqcloud.com/path/to/file.pdf?...",
    "key": "path/to/file.pdf",
    "expires": "2024-03-21T10:30:00Z"
  }
}
```

---

### Files Skills

#### `files.upload` - 上传文件

**输入（单文件）**
```json
{
  "key": "docs/report.pdf",
  "content_base64": "SGVsbG8gV29ybGQ=",
  "content_type": "application/pdf",
  "enqueue_to_repo": "HIT-A/HITA_RagData",
  "enqueue_branch": "main"
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "success": true,
    "file_id": "docs/report.pdf",
    "size": 1024,
    "url": "https://...",
    "source": "cos",
    "queued_path": "incoming/manual_raw/abc123_report.pdf"
  }
}
```

---

#### `files.download` - 下载文件

**输入**
```json
{
  "key": "docs/report.pdf",
  "format": "base64",
  "expires_seconds": 3600
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "success": true,
    "content_base64": "SGVsbG8gV29ybGQ=",
    "size": 1024,
    "key": "docs/report.pdf"
  }
}
```

---

### Search Skills

#### `search` - 统一搜索

**输入**
```json
{
  "query": "机器学习",
  "sources": ["rag", "brave", "annas", "arxiv", "github", "cos", "course", "course_read", "hit_teacher"],
  "top_k": 10,
  "summarize": true,
  "lang": "zh",
  "timeout": 30
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "query": "机器学习",
    "total": 25,
    "results": [
      {
        "source": "rag",
        "source_type": "knowledge",
        "title": "机器学习基础",
        "content": "机器学习是人工智能的一个分支...",
        "url": "https://...",
        "score": 0.95,
        "metadata": {"doc_id": "...", "chunk_id": "..."}
      }
    ],
    "by_source": {"rag": 10, "brave": 8, "course": 5, "arxiv": 2},
    "by_source_type": {"knowledge": 10, "web": 8, "course": 5, "paper": 2},
    "summary": "根据搜索结果，机器学习相关资料包括...",
    "key_points": ["RAG 知识库有 10 条相关记录", "课程资料 5 条"],
    "duration_ms": 1234,
    "errors": []
  }
}
```

---

#### `aggregator.summarize` - AI 总结（异步）

**输入**
```json
{
  "query": "机器学习课程怎么样",
  "results": [
    {"source": "course", "title": "CS301", "content": "机器学习课程内容...", "score": 0.9}
  ],
  "style": "concise",
  "max_length": 500,
  "language": "zh",
  "context_hint": "用户想了解课程质量"
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "summary": "根据搜索结果，这门机器学习课程...",
  "key_points": ["课程内容全面", "教师经验丰富"],
  "sources_used": ["course", "rag"],
  "confidence": 0.92,
  "token_used": 1500,
  "duration_ms": 2500
}
```

---

### Data Skills

#### `data.ingest` - 统一数据接入（异步）

**输入**
```json
{
  "source_type": "manual",
  "source_name": "课程笔记",
  "content": "# 笔记内容...",
  "file_path": "notes/lesson1.md",
  "target_repo": "HIT-A/HITA_RagData",
  "target_branch": "main",
  "store_in_cos": true,
  "cos_prefix": "rag-content",
  "auto_ingest_rag": true,
  "overwrite": false
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "status": "completed",
  "file_count": 1,
  "chunks_count": 5,
  "cos_keys": ["rag-content/manual/课程笔记.md"],
  "markdown_path": "sources/normalized/...",
  "vectorized": true
}
```

---

#### `data.ingest_batch` - 批量数据接入（异步）

**输入**
```json
{
  "items": [
    {"source_type": "manual", "source_name": "笔记1", "content": "..."},
    {"source_type": "github", "source_name": "repo-data", "source_url": "https://github.com/..."}
  ],
  "target_repo": "HIT-A/HITA_RagData",
  "store_in_cos": true,
  "auto_ingest_rag": true,
  "workers": 4
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

---

### Crawl4AI Skills

#### `crawl4ai.page` - 抓取单页

**输入**
```json
{
  "url": "https://example.com/article",
  "wait_for": "",
  "exclude_paths": ["/api/", "/admin/"],
  "content_filter": true,
  "output_format": "markdown",
  "queue_to_intake": true,
  "intake_repo": "HIT-A/HITA_RagData",
  "intake_branch": "main",
  "intake_skill_dir": "incoming/crawl_raw"
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "markdown": "# Article Title\n\n文章内容...",
    "html": "<html>...</html>",
    "intake": {
      "enabled": true,
      "queued": true,
      "path": "incoming/crawl_raw/..."
    }
  }
}
```

---

#### `crawl4ai.site` - 全站抓取（异步）

**输入**
```json
{
  "start_url": "https://example.com",
  "max_pages": 100,
  "max_depth": 3,
  "include_patterns": ["*.html"],
  "exclude_patterns": ["*admin*"],
  "content_filter": true,
  "use_sitemap": true,
  "concurrent_requests": 5,
  "store_in_cos": true,
  "cos_prefix": "crawl/example",
  "auto_ingest_to_rag": true,
  "rag_source": "crawl"
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "crawl_id": "abc123",
  "status": "completed",
  "pages_crawled": 45
}
```

---

#### `crawl4ai.status` - 查询抓取状态

**输入**
```json
{
  "crawl_id": "abc123"
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "crawl_id": "abc123",
    "status": "completed",
    "pages_crawled": 45,
    "results": [
      {"url": "https://example.com/page1", "status": "success"}
    ]
  }
}
```

---

### GitHub Skills

#### `github.batch_download` - 批量下载 GitHub（异步）

**输入**
```json
{
  "repos": [
    {"org": "HIT-A", "repo": "agent-backend", "ref": "main"}
  ],
  "file_types": [".md", ".txt", ".pdf"],
  "max_file_size": 10485760,
  "max_repo_size_mb": 500,
  "max_files": 100,
  "convert_to_md": true,
  "push_to_github": true,
  "target_repo": "HIT-A/HITA_RagData",
  "target_branch": "main",
  "store_in_cos": true,
  "cos_prefix": "github-downloads",
  "auto_ingest_rag": true,
  "rag_collection": "hit_courses_2048",
  "queue_to_intake": true,
  "intake_skill_dir": "incoming/download_raw",
  "workers": 4
}
```

**输出**
```json
{
  "ok": true,
  "job_id": "uuid..."
}
```

**Job 完成输出**
```json
{
  "total_repos": 1,
  "total_files": 50,
  "success_count": 48,
  "fail_count": 2,
  "intake_enqueued": 48,
  "total_size_mb": 25.5,
  "duration_ms": 5000,
  "files": [...]
}
```

---

#### `document.convert` - 文档格式转换

**输入**
```json
{
  "content_base64": "base64编码的PDF...",
  "filename": "report.pdf",
  "chunking_strategy": "by_title",
  "max_characters": 1500
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "markdown": "# Report Title\n\nContent...",
    "chunks": [
      {"text": "Chunk 1 content...", "chunk_id": "0"},
      {"text": "Chunk 2 content...", "chunk_id": "1"}
    ]
  }
}
```

---

### MCP Skills

#### `mcp.list_servers` - 列出 MCP 服务器

**输入**: 无

**输出**
```json
{
  "ok": true,
  "output": {
    "servers": [
      {
        "name": "arxiv",
        "transport": "stdio",
        "initialized": true,
        "tools": [{"name": "search_arxiv", "description": "..."}],
        "resources": []
      }
    ]
  }
}
```

---

#### `mcp.list_tools` - 列出 MCP 工具

**输入**
```json
{
  "server": "arxiv"
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "server": "arxiv",
    "tools": [
      {"name": "search_arxiv", "description": "Search arXiv for papers", "inputSchema": {...}}
    ]
  }
}
```

---

#### `mcp.call_tool` - 调用 MCP 工具

**输入**
```json
{
  "server": "arxiv",
  "tool": "search_arxiv",
  "arguments": {
    "query": "machine learning",
    "max_results": 10
  }
}
```

**输出**
```json
{
  "ok": true,
  "output": {
    "content": [
      {
        "type": "text",
        "text": "{\"papers\": [...]}"
      }
    ]
  }
}
```

---

## MCP Servers（默认注册）

| Server | 功能 |
|--------|------|
| `sequential-thinking` | 顺序思考 |
| `annas-archive` | 文献搜索 |
| `arxiv` | 论文搜索 |
| `brave-search` | 网页搜索 |
| `crawl4ai` | 网页爬取 |
| `unstructured` | 文档格式转换 |

---

## 变更历史

### 2026-03-21 (第二轮修复)
- 修复 `document.convert` nil 参数传递问题
- 修复 `data.ingest` 的 nil map panic bug
- 修复 `sources.json` 解析时 nil map 未初始化问题
- 修复 `parseAnnasTextResults` 不处理 raw_result 的问题
- 修复 Python unstructured server 语法错误
- 安装 unstructured Python 依赖（PyMuPDF, python-docx, python-pptx）
- 确认所有 9 个 search 子数据源正常工作

### 2026-03-21 (第一轮修复)
- 修复 `summarizeWithLLM` 接入 LLM 总结
- 修复 `processCrawlSource` 实现爬虫来源处理
- 修复 `processedChunks` atomic 操作 bug
- 添加 `unstructured` MCP 服务器默认注册
- 删除已废弃的 `cos.get_quota` skill

### 2026-03-18
- Skills 总数更新为 33（包含 `rag.intake_manual_folder`）
- 动态 MCP 注册能力已下线
- PR 链路对齐新版 pr-server
- `rag.sync_to_repo` 增强 Git 运行机制

---

## 系统设计

### 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                      agent-backend                          │
│                      (Go HTTP Server)                       │
├─────────────────────────────────────────────────────────────┤
│  Skills Layer (32 skills)                                   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│  │ RAG    │ │ Search  │ │  PR    │ │ Course │          │
│  │ 5个    │ │ 2个     │ │ 3个    │ │ 2个    │          │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘          │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│  │ COS    │ │ Crawl  │ │  GitHub │ │  MCP   │          │
│  │ 4个    │ │ 3个     │ │ 2个     │ │ 3个    │          │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘          │
├─────────────────────────────────────────────────────────────┤
│  Service Layer                                             │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐      │
│  │  COS Storage │ │  RAG Client  │ │ GitHub Fetcher│      │
│  │  (腾讯云SDK) │ │ (Qdrant API) │ │  (REST API)  │      │
│  └──────────────┘ └──────────────┘ └──────────────┘      │
├─────────────────────────────────────────────────────────────┤
│  MCP Layer (6 servers)                                      │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                     │
│  │ crawl4ai│ │  brave  │ │  arxiv  │                     │
│  │ (网页爬取)│ │(网页搜索)│ │ (论文搜索)│                     │
│  └─────────┘ └─────────┘ └─────────┘                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                     │
│  │ annas   │ │unstruct │ │sequential│                     │
│  │(文献搜索)│ │(文档转换)│ │(顺序思考) │                     │
│  └─────────┘ └─────────┘ └─────────┘                     │
└─────────────────────────────────────────────────────────────┘
```

### 数据流

#### RAG 数据 Pipeline

```
用户上传文档
    │
    ▼
data.ingest / files.upload
    │
    ├─→ COS Storage (永久存储)
    │
    └─→ GitHub Raw Repo (入队)
            │
            ▼
    rag.intake_manual_folder (定时/手动)
            │
            ├─→ Parse Document (markdown)
            │
            ├─→ Embedding (BigModel API)
            │
            └─→ Qdrant (向量数据库)
                    │
                    ▼
            rag.query / search
```

#### Search 聚合 Pipeline

```
用户查询
    │
    ▼
search (聚合入口)
    │
    ├─→ rag (Qdrant 向量搜索)
    │
    ├─→ brave (Brave Search API)
    │
    ├─→ github (GitHub Code Search API)
    │
    ├─→ arxiv (arXiv MCP)
    │
    ├─→ annas (Anna's Archive MCP)
    │
    ├─→ cos (COS 文件列表)
    │
    ├─→ course (pr-server 课程搜索)
    │
    ├─→ course_read (pr-server README 读取)
    │
    └─→ hit_teacher (crawl4ai 爬取教师主页)
            │
            ▼
    aggregator.summarize (LLM 总结)
            │
            ▼
        最终结果
```

### 外部依赖

| 服务 | 用途 | 环境变量 |
|------|------|---------|
| 腾讯云 COS | 文件存储 | `COS_SECRET_ID`, `COS_SECRET_KEY`, `COS_BUCKET`, `COS_REGION` |
| Qdrant | 向量数据库 | `QDRANT_URL`, `QDRANT_API_KEY` |
| BigModel API | 文本嵌入/总结 | `BIGMODEL_API_KEY` |
| pr-server | 课程/PR 服务 | `PR_SERVER_URL`, `PR_SERVER_TOKEN` |
| Brave Search | 网页搜索 | `BRAVE_API_KEY` |
| GitHub | 代码仓库 | `GITHUB_TOKEN` |
| crawl4ai MCP | 网页爬取 | Python 环境 |
| unstructured MCP | 文档转换 | Python 环境 |

### 已知限制

1. **hit.teacher**: 哈工大教师主页需登录才能获取完整简历，当前只能抓取公开的基础信息（姓名、部门、联系方式）
2. **COS files.download**: 需要正确的 bucket 权限才能下载
3. **Anna's Archive**: 返回纯文本格式，需要客户端解析

### 测试状态

所有 32 个 skill 和 9 个 search 子数据源均已测试通过。
